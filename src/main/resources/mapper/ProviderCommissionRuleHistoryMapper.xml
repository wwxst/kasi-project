<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kasi.backend.drama.mapper.ProviderCommissionRuleHistoryMapper">
    <resultMap id="HistoryMap" type="com.kasi.backend.drama.entity.ProviderCommissionRuleHistory">
        <id column="id" property="id"/>
        <result column="provider_id" property="providerId"/>
        <result column="rule_id" property="ruleId"/>
        <result column="channel_fee_rate" property="channelFeeRate"/>
        <result column="principal_fee_rate" property="principalFeeRate"/>
        <result column="principal_commission_rate" property="principalCommissionRate"/>
        <result column="downstream_fee_rate" property="downstreamFeeRate"/>
        <result column="downstream_commission_rate" property="downstreamCommissionRate"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO provider_commission_rule_history
        (provider_id, rule_id, channel_fee_rate, principal_fee_rate, principal_commission_rate,
         downstream_fee_rate, downstream_commission_rate, created_by)
        VALUES (#{providerId}, #{ruleId}, #{channelFeeRate}, #{principalFeeRate},
                #{principalCommissionRate}, #{downstreamFeeRate}, #{downstreamCommissionRate}, #{createdBy})
    </insert>

    <select id="findLatestByProviderId" resultMap="HistoryMap">
        SELECT * FROM provider_commission_rule_history WHERE provider_id=#{providerId}
        ORDER BY id DESC LIMIT 1
    </select>

    <select id="findAllByProviderId" resultMap="HistoryMap">
        SELECT * FROM provider_commission_rule_history WHERE provider_id=#{providerId}
        ORDER BY id ASC
    </select>
</mapper>
