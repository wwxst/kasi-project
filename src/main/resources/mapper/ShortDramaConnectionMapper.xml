<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kasi.backend.provider.mapper.ShortDramaConnectionMapper">

    <select id="findByProviderId" resultType="com.kasi.backend.provider.entity.ShortDramaConnection">
        SELECT * FROM short_drama_connection WHERE provider_id = #{providerId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO short_drama_connection
            (provider_id, connection_name, partner_id, api_key_ciphertext, currency, status,
             created_by, updated_by)
        VALUES
            (#{providerId}, #{connectionName}, #{partnerId}, #{apiKeyCiphertext}, #{currency}, #{status},
             #{createdBy}, #{updatedBy})
    </insert>

    <update id="update">
        UPDATE short_drama_connection
        SET connection_name = #{connectionName},
            partner_id = #{partnerId},
            <if test="apiKeyCiphertext != null">
                api_key_ciphertext = #{apiKeyCiphertext},
            </if>
            currency = #{currency},
            status = #{status},
            updated_by = #{updatedBy},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
