# 多短剧平台推广模块路线图

日期：2026-08-17

当前状态：模块 1 至模块 4 后端已实现并通过自动化验证；模块 5 至模块 7 尚未开始。

设计规格：[多短剧平台推广业务设计规格](../specs/2026-08-17-multi-drama-provider-promotion-design.md)

## 执行原则

- 一次只实现、测试和验收一个模块。
- 当前模块没有通过完整回归测试和用户验收前，不开始下一模块。
- 每个模块使用独立实施计划和独立提交；除用户明确批准的共享数据库基础外，不能把后续模块的表、接口或抽象提前混入。
- README 和 AGENTS.md 只记录已经实现并验证的能力；未完成内容留在设计规格和路线图中。
- 所有模块继续遵循当前 Controller、Service 接口与 `impl` 实现、Mapper、Entity、DTO、VO 分层。

## 模块顺序

### 模块 1：短剧平台接入账号与 GoodShort 通信基础

交付：

- 短剧平台和单平台单接入账号的数据结构。
- GoodShort `pid + key` 的安全保存和脱敏展示。
- GoodShort 固定向量签名实现。
- 管理员查看、配置和测试 GoodShort 接入账号的后端接口。
- 平台能力声明基础；GoodShort 明确不支持锚点。

不包含：短剧同步、媒体账号、报备、素材、推广链接、订单、佣金和转化数据。

### 模块 2：GoodShort 短剧目录

依赖模块 1。交付全量/增量同步、同步检查点、后台列表、上下架和同步状态。此模块只服务管理员，不开放推广用户选剧页面。

### 模块 3：媒体账号与 GoodShort 报备

依赖模块 1。交付推广用户媒体账号绑定、全局唯一归属、不可转让、资料修正、停用、GoodShort 报备和审核状态轮询；业务状态只使用“审核中、已加白、已失败”。

当前状态：后端已实现并持续纳入全量回归；本次完整套件 263 项测试通过。用户和管理员 API、GoodShort 提交/查询适配器、持久任务重试和删除保护已交付，管理前端待实现。

### 模块 4：短剧平台分佣规则

依赖模块 2。已交付 V8 `provider_commission_rule`、按短剧平台和生效时间配置的五项费率、平台内统一规则、左闭右开时间段不重叠校验、规则状态生命周期、内部有效规则查询、管理员 API 和 `BigDecimal` 计算器。当前和未来增加的接入账号及平台下所有短剧共用平台规则；本模块不接推广链接或订单，“无有效规则不可推广”由模块 5 在生成链接时接入。详细设计见 [短剧平台分佣规则设计](../specs/2026-08-21-provider-commission-rule-design.md)。

当前状态：后端已实现并通过自动化验证。普通管理员可只读查询，超级管理员可写；`PENDING` 可编辑/删除，`ACTIVE` 只能提前结束，`ENDED` 只读。

### 模块 5：素材与推广链接

依赖模块 2、3、4。交付短剧详情、免费内容播放、单集/批量下载任务，以及同一账号和短剧生成多条推广链接。GoodShort 使用本地 `tracking_no` 作为 `customParams`。

### 模块 6：订单、预计佣金与导出

依赖模块 4、5。交付每 5 分钟轮询、历史复核、订单幂等归因、未支付/已支付/已退款状态、费率快照、预计佣金和管理员 Excel 导出。不建设钱包、提现和已结算状态。

### 模块 7：每日转化数据

依赖模块 5。交付按推广链接和自然日同步点击、注册、充值、订单等指标，以及推广用户本人数据和管理员全量数据查询。

## 当前状态

- 总体设计：已确认并提交。
- 模块 1：已实现并通过自动化验证。
- 模块 2：已实现 GoodShort 全量/增量同步、断点恢复、数据库租约、管理员目录 API 和定时调度。
- 模块 3：后端已实现并通过自动化验证；前端尚未开始。
- 模块 4：平台级规则持久化、版本生命周期、并发重叠保护、管理 API 和纯计算器已实现并通过自动化验证；推广链接接入和订单费率快照不属于本模块。
- 模块 5 至模块 7：仅冻结边界，尚未进入实现。
