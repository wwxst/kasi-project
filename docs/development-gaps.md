# 当前开发缺口

更新时间：2026-08-24

本文只记录当前代码尚未实现的能力，不代表已经批准的开发路线图。

## 短剧 CPS 快速上线版已完成

- GoodShort 短剧目录、媒体账号报白和真实推广链接/口令。
- 管理员按不超过 31 天的窗口手动同步 GoodShort 订单。
- `(connection_id, external_order_id)` 幂等、原始 JSON 留存和未知状态留存。
- `customParams -> promotion_link.tracking_no -> user_id` 唯一归因链路。
- 平台五费率历史快照、订单费率快照、`BigDecimal` 佣金和退款冲销。
- 管理员订单查询/CSV；用户本人按支付月份查询、汇总和 CSV。

## 明确后置

- GoodShort 订单定时同步、同步检查点、租约和自动补偿窗口。
- 未归因订单人工改归属工作流。
- 按支付时间自动匹配生效费率版本和完整规则时间线 UI。
- 正式月度账单实体、锁定、对账、开票、付款状态和审计流转。
- 钱包余额、提现申请、付款通道和财务出款。
- 点击、注册、充值等转化分析和 `PromotionTask` 聚合。
- CapCut、CPA、CPM 和通用项目管理。

快速上线版的月度佣金只是按 `promotion_order.paid_at` 聚合的只读结果，不是已确认或已付款账单。
