# 平台默认分佣规则管理端设计

状态：已确认并改为默认配置模型

分佣规则不再嵌入“短剧 API 配置”页，而是独立放在“系统配置 → 分佣规则”表格页。表格按平台展示五项费率，可按平台筛选。

- 每个平台最多一条默认分佣规则。
- 无生效时间、结束时间和状态字段。
- 未配置平台由超级管理员执行“设置默认规则”。
- 已配置平台由超级管理员执行“编辑”，保存后直接覆盖原费率。
- 不提供删除、提前结束或历史版本操作。
- 普通管理员只读。
- API 配置页只保留 URL、PID、KEY、启用状态和连接测试。
