<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kasi.backend.provider.mapper.ShortDramaConnectionMapper">

    <select id="findById" resultType="com.kasi.backend.provider.entity.ShortDramaConnection">
        SELECT * FROM short_drama_connection WHERE id = #{id}
    </select>

    <select id="lockById" resultType="com.kasi.backend.provider.entity.ShortDramaConnection">
        SELECT * FROM short_drama_connection WHERE id = #{id} FOR UPDATE
    </select>

    <select id="findByProviderId" resultType="com.kasi.backend.provider.entity.ShortDramaConnection">
        SELECT * FROM short_drama_connection WHERE provider_id = #{providerId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO short_drama_connection
            (provider_id, connection_name, base_url, media_root_domain, partner_id, api_key_ciphertext, currency, status,
             created_by, updated_by)
        VALUES
            (#{providerId}, #{connectionName}, #{baseUrl}, #{mediaRootDomain}, #{partnerId}, #{apiKeyCiphertext}, #{currency},
             #{status},
             #{createdBy}, #{updatedBy})
    </insert>

    <update id="update">
        UPDATE short_drama_connection
        SET connection_name = #{connectionName},
            base_url = #{baseUrl},
            media_root_domain = #{mediaRootDomain},
            partner_id = #{partnerId},
            <if test="apiKeyCiphertext != null">
                api_key_ciphertext = #{apiKeyCiphertext},
            </if>
            currency = #{currency},
            status = #{status},
            updated_by = #{updatedBy},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
