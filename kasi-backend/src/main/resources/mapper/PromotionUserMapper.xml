<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kasi.backend.user.mapper.PromotionUserMapper">

    <resultMap id="BaseResultMap" type="com.kasi.backend.user.entity.PromotionUser">
        <id column="id" property="id"/>
        <result column="user_no" property="userNo"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="real_name" property="realName"/>
        <result column="mobile" property="mobile"/>
        <result column="email" property="email"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="status" property="status"/>
        <result column="register_source" property="registerSource"/>
        <result column="last_login_at" property="lastLoginAt"/>
        <result column="last_login_ip" property="lastLoginIp"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM promotion_user WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByMobile" resultMap="BaseResultMap">
        SELECT * FROM promotion_user WHERE mobile = #{mobile} AND deleted_at IS NULL
    </select>

    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT * FROM promotion_user WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findByUserNo" resultMap="BaseResultMap">
        SELECT * FROM promotion_user WHERE user_no = #{userNo} AND deleted_at IS NULL
    </select>

    <select id="findByAccount" resultMap="BaseResultMap">
        SELECT * FROM promotion_user
        WHERE deleted_at IS NULL
          AND (mobile = #{account} OR email = #{account})
        LIMIT 1
    </select>

    <select id="findByAccountForUpdate" resultMap="BaseResultMap">
        SELECT * FROM promotion_user
        WHERE deleted_at IS NULL
          AND (mobile = #{account} OR email = #{account})
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="findByIdForUpdate" resultMap="BaseResultMap">
        SELECT * FROM promotion_user WHERE id = #{id} AND deleted_at IS NULL FOR UPDATE
    </select>

    <select id="countByKeyword" resultType="long">
        SELECT COUNT(*) FROM promotion_user
        WHERE deleted_at IS NULL
          AND (#{keyword} IS NULL
            OR user_no LIKE CONCAT('%', #{keyword}, '%')
            OR mobile LIKE CONCAT('%', #{keyword}, '%')
            OR email LIKE CONCAT('%', #{keyword}, '%')
            OR nickname LIKE CONCAT('%', #{keyword}, '%')
            OR real_name LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <select id="findPage" resultMap="BaseResultMap">
        SELECT * FROM promotion_user
        WHERE deleted_at IS NULL
          AND (#{keyword} IS NULL
            OR user_no LIKE CONCAT('%', #{keyword}, '%')
            OR mobile LIKE CONCAT('%', #{keyword}, '%')
            OR email LIKE CONCAT('%', #{keyword}, '%')
            OR nickname LIKE CONCAT('%', #{keyword}, '%')
            OR real_name LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <insert id="insert" parameterType="com.kasi.backend.user.entity.PromotionUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO promotion_user (user_no, password, nickname, real_name, mobile, email,
                                    avatar_url, status, register_source, remark)
        VALUES (#{userNo}, #{password}, #{nickname}, #{realName}, #{mobile}, #{email},
                #{avatarUrl}, #{status}, #{registerSource}, #{remark})
    </insert>

    <update id="updatePassword">
        UPDATE promotion_user SET password = #{password} WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE promotion_user
        SET last_login_at = #{lastLoginAt}, last_login_ip = #{lastLoginIp}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE promotion_user SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateUserNo">
        UPDATE promotion_user
        SET user_no = #{userNo}, nickname = COALESCE(nickname, #{nickname})
        WHERE id = #{id}
    </update>

    <update id="updateProfile" parameterType="com.kasi.backend.user.entity.PromotionUser">
        UPDATE promotion_user
        SET nickname = #{nickname}, real_name = #{realName}, mobile = #{mobile}, email = #{email},
            avatar_url = #{avatarUrl}, remark = #{remark}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM promotion_user WHERE id = #{id}
    </delete>

</mapper>
