<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kasi.backend.admin.mapper.SysAdminUserMapper">

    <resultMap id="BaseResultMap" type="com.kasi.backend.admin.entity.SysAdminUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="mobile" property="mobile"/>
        <result column="email" property="email"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="department_id" property="departmentId"/>
        <result column="status" property="status"/>
        <result column="is_super_admin" property="isSuperAdmin"/>
        <result column="last_login_at" property="lastLoginAt"/>
        <result column="last_login_ip" property="lastLoginIp"/>
        <result column="password_changed_at" property="passwordChangedAt"/>
        <result column="remark" property="remark"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM sys_admin_user WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM sys_admin_user WHERE username = #{username} AND deleted_at IS NULL
    </select>

    <select id="findByMobile" resultMap="BaseResultMap">
        SELECT * FROM sys_admin_user WHERE mobile = #{mobile} AND deleted_at IS NULL
    </select>

    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT * FROM sys_admin_user WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findByAccount" resultMap="BaseResultMap">
        SELECT * FROM sys_admin_user
        WHERE deleted_at IS NULL
          AND (username = #{account} OR mobile = #{account} OR email = #{account})
        LIMIT 1
    </select>

    <select id="findByAccountForUpdate" resultMap="BaseResultMap">
        SELECT * FROM sys_admin_user
        WHERE deleted_at IS NULL
          AND (username = #{account} OR mobile = #{account} OR email = #{account})
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="findByIdForUpdate" resultMap="BaseResultMap">
        SELECT * FROM sys_admin_user WHERE id = #{id} AND deleted_at IS NULL FOR UPDATE
    </select>

    <select id="countByKeyword" resultType="long">
        SELECT COUNT(*)
        FROM sys_admin_user
        WHERE deleted_at IS NULL
          AND (#{keyword} IS NULL
            OR username LIKE CONCAT('%', #{keyword}, '%')
            OR real_name LIKE CONCAT('%', #{keyword}, '%')
            OR mobile LIKE CONCAT('%', #{keyword}, '%')
            OR email LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <select id="findPage" resultMap="BaseResultMap">
        SELECT *
        FROM sys_admin_user
        WHERE deleted_at IS NULL
          AND (#{keyword} IS NULL
            OR username LIKE CONCAT('%', #{keyword}, '%')
            OR real_name LIKE CONCAT('%', #{keyword}, '%')
            OR mobile LIKE CONCAT('%', #{keyword}, '%')
            OR email LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <insert id="insert" parameterType="com.kasi.backend.admin.entity.SysAdminUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_admin_user (username, password, real_name, mobile, email, avatar_url,
                                    department_id, status, is_super_admin, remark, created_by, updated_by)
        VALUES (#{username}, #{password}, #{realName}, #{mobile}, #{email}, #{avatarUrl},
                #{departmentId}, #{status}, #{isSuperAdmin}, #{remark}, #{createdBy}, #{updatedBy})
    </insert>

    <update id="updateProfile" parameterType="com.kasi.backend.admin.entity.SysAdminUser">
        UPDATE sys_admin_user
        SET username = #{username},
            real_name = #{realName},
            mobile = #{mobile},
            email = #{email},
            avatar_url = #{avatarUrl},
            department_id = #{departmentId},
            remark = #{remark},
            updated_by = #{updatedBy},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE sys_admin_user
        SET password = #{password}, password_changed_at = #{passwordChangedAt}
        WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE sys_admin_user
        SET last_login_at = #{lastLoginAt}, last_login_ip = #{lastLoginIp}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE sys_admin_user SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
