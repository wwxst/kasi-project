# 当前尚未规范的工程事项

本清单只记录“规则或契约尚未统一”，不代表对应业务功能已经批准或正在实现。完成某项后，应补充证据并将状态移入相关文档或 ADR；不要仅为清空清单而提前实现功能。

## P0：影响安全、数据正确性或发布判断

| 事项 | 当前缺口 | 下一步 | 状态 |
| --- | --- | --- | --- |
| API 错误契约 | HTTP 状态与 `ApiResponse.code` 的组合、校验错误和业务错误的边界需要形成统一契约 | 新增 API 错误契约 ADR，并补齐 Controller 回归矩阵 | 待规范 |
| 外部平台失败策略 | 超时、重试、限流、熔断、不可重试错误和凭据失效的统一行为未形成跨适配器规范 | 先盘点 GoodShort 现状，再形成适配器失败策略 ADR | 待规范 |
| 敏感操作审计 | 密码、账号状态、密钥、报备和删除操作的审计字段、留存和脱敏要求未统一 | 定义安全审计事件与日志脱敏规范 | 待规范 |
| 生产配置启动校验 | profile、密钥、真实验证码 sender、Redis/MySQL 可用性和安全失败边界缺少一份启动前检查表 | 补充配置矩阵和启动检查文档 | 待规范 |

## P1：影响跨模块协作和长期维护

| 事项 | 当前缺口 | 下一步 | 状态 |
| --- | --- | --- | --- |
| API 兼容与版本策略 | 未统一未发布 API 的破坏性变更、已发布 API 的弃用、版本和变更记录 | 结合发布阶段建立 API 演进 ADR | 待规范 |
| 异步任务一致性 | 幂等键、租约、重试、死信/人工重试、重复执行和状态机在不同任务间的共性规则未统一 | 提炼任务生命周期与幂等规范，不提前引入通用框架 | 待规范 |
| Redis/MySQL 一致性 | `MUTATING`、事务提交后清理和 Redis 故障安全失败已有局部约定，缺少跨模块检查表 | 用现有认证和报备流程形成一致性 ADR | 待规范 |
| 并发与锁定 | 平台行锁、唯一键、租约和重试的适用边界及隔离级别未集中说明 | 记录并发控制决策和测试要求 | 待规范 |
| 迁移生命周期 | 开发阶段可重建与未来生产增量迁移的切换条件、回滚限制和验收证据未形成统一流程 | 补充 Flyway 发布/重建规范 | 待规范 |
| 测试分层 | 单元、Mapper、Controller、迁移、外部适配器和完整套件的选择标准及环境依赖未统一 | 建立测试分层矩阵和失败归因规则 | 待规范 |
| 时间、金额和标识 | 时区、时间精度、百分比/比例、金额舍入、公开编号与内部 ID 的通用约定分散在模块文档 | 汇总为数据契约规范，禁止复制冲突规则 | 待规范 |
| 当前契约漂移 | `AGENTS.md`、`README.md` 和专项文档存在旧版与当前分佣规则描述并存的情况，容易把历史方案误读为现状 | 以当前实现和测试为依据逐项清理旧描述，并在 ADR 索引中标注替代关系 | 待规范 |

## P2：影响交接、可观察性和文档质量

| 事项 | 当前缺口 | 下一步 | 状态 |
| --- | --- | --- | --- |
| 日志、指标和链路 | 关键业务事件、任务耗时、远端调用结果和关联 ID 的最低可观察性未统一 | 先定义字段与脱敏规则，再按模块补充 | 待规范 |
| 数据保留与恢复 | 业务数据、任务历史、凭据密文、日志和备份恢复演练的保留边界未统一 | 形成数据生命周期与恢复目标文档 | 待规范 |
| 分支与发布 | 分支保护、评审、提交粒度、发布标签、回滚和远程 parity 证据未形成仓库级流程 | 补充 Git/发布检查表，沿用现有提交规范 | 待规范 |
| 文档过期检查 | README、AGENTS、设计/计划文档之间的当前/规划漂移主要依赖人工发现 | 增加轻量链接、占位符和状态检查，不引入复杂文档平台 | 待规范 |
| 变更模板 | 缺少统一的问题根因、非目标、验证证据和 ADR 关联模板 | 提供 Issue/PR 描述模板，待确认后再加入仓库 | 待规范 |

## 处理顺序

先处理 P0 中的安全、数据正确性和外部失败契约，再处理 P1 的跨模块规则；P2 只在有具体交付场景时推进。每次只选择一个清晰事项，按 `DEVELOPMENT.md` 的 L2/L3 闸门执行，并在完成后更新本清单。
